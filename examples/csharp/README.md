# Hello em C#

Dependência: só o .NET SDK 8+ (sem pacotes NuGet).

```bash
dotnet run               # porta padrao 8765
dotnet run -- 9000       # porta customizada
```

Antes, deixe o browser servindo (na raiz do projeto):

```bash
python3 browser.py --servir
```
